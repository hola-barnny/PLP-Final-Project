name: parent_teacher_communication
description: "A new Flutter project."

# Prevents accidental publishing
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ensures compatibility with Dart 3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3 # Firebase authentication
  cloud_firestore: ^5.5.0 # Cloud Firestore
  firebase_messaging: ^15.1.5 # Firebase Cloud Messaging
  firebase_data_connect: ^0.1.2+4
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/