name: parent_teacher_communication
description: "A new Flutter project."

# Prevents accidental publishing
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ensures compatibility with Dart 3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2 # Updated to a valid and compatible version
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add assets here if needed
  assets:
    - assets/images/ # Example path for images